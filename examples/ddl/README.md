# Example 01: Basic Schema (MySQL)

Demonstrates the creation of a standalone table with standard data types and constraints.

## Execution
1. Open DBeaver.
2. Connect to the MySQL development database.
3. Open `schema.sql` and run the script.